# Auto DM

AI-powered solo D&D 5e game master. Um jogador humano, party de companheiros controlados por IA, e o mestre é inteiramente a IA.

> Veja `SPEC.md` para a especificação completa e `PLAN.md` para o plano por fases.

---

## Princípios arquiteturais inegociáveis

1. **Mecânica é autoritativa.** O motor de regras em Python é a fonte da verdade. O LLM **narra**, mas nunca decide mecânica. Toda ação de combate passa por validação e execução no engine.
2. **LLM propõe, engine dispõe.** Companheiros e jogador enviam intenções (texto livre ou JSON estruturado), engine valida se a ação é possível, executa rolagens, aplica dano, atualiza estado, e devolve resultado. LLM só vê o resultado pra narrar.
3. **Contexto é gerenciado ativamente.** Resumos periódicos evitam estourar tokens em campanhas longas.
4. **Configurável por design.** Provider, modelo, temperatura, idioma, nível de narração — nada hardcoded de forma oculta.

## Stack

- **Python 3.11+**
- **Pydantic** para modelos de estado e validação
- **FastAPI + Uvicorn** para o backend web (auth, sessões, REST, SSE)
- **LangChain/LangGraph** para orquestração de agentes (DM + companheiros)
- **Providers LLM**: Claude, OpenAI, Gemini, GLM, **Minimax** (provider ativo do usuário — única chave disponível)
- **PHB 5e** em `data/phb/` como fonte de verdade para regras (não hardcoded em Python)

## Estrutura do projeto

```
src/auto_dm/
├── llm/          # abstração de providers (Protocol + adapters)
├── engine/       # motor de regras (Python puro, SEM LLM)
├── state/        # modelos Pydantic (Character, GameState, Action...)
├── phb/          # loader do SRD 5.1 (parser, models, lookup)
├── character/    # CharacterBuilder + spell selection
├── agents/       # DM agent + companion agents
├── companions/   # pre-defined companion roster + party roll/synergy
├── persistence/  # save/load JSON
└── web/          # backend FastAPI (auth, sessões, REST, SSE) + static/
```

Raiz do repo: `Dockerfile`, `docker-compose.yml` (prod), `docker-compose.dev.yml`
(dev com Postgres+Redis+backend), `DEPLOY.md` (deploy completo).

## Comandos úteis

### Rodar o projeto (Docker — forma principal)

```bash
# Dev: sobe Postgres + Redis + backend; frontend em http://localhost:14004
cp .env.example .env          # preencher JWT_SECRET (≥32) e AUTO_DM_API_KEY
docker compose -f docker-compose.dev.yml up --build

# Prod: backend only (Postgres+Redis externos), bind 127.0.0.1:4004
docker compose up -d --build
docker compose logs -f auto-dm

# Tear down
docker compose -f docker-compose.dev.yml down      # dev (mantém volume)
docker compose -f docker-compose.dev.yml down -v   # dev + apaga dados
docker compose down                                # prod
```

Variáveis via `.env` (compose interpola `${VAR}`): `JWT_SECRET`,
`AUTO_DM_API_KEY`/`AUTO_DM_PROVIDER`/`AUTO_DM_MODEL`,
`DATABASE_URL`, `REDIS_URL`, `FRONTEND_URL`, `INVITE_CODE`. Detalhes de
deploy (nginx, TLS, Vercel, backups) em `DEPLOY.md`.

### Desenvolvimento local (sem Docker)

```bash
pip install -e ".[dev]"   # instalar em modo dev
pytest                     # rodar testes
ruff check .               # lint
```

> **Provider ativo é Minimax** — o adapter é carregado do ambiente via
> `LLMConfig.from_env(prefix="AUTO_DM_")`. Não há adapter novo pra
> implementar; o backend web lê as vars `AUTO_DM_*`.

## Regras para Claude Code

- **Nunca ler `.env`** — contém API keys. Usar `.env.example` como referência de template.
- **`data/phb/` é leitura livre** — esses `.md` são a fonte de regras. Conteúdo derivado do D&D 5e **SRD v5.1** (Open Game License + CC BY 4.0) — não é o PHB completo. Arquivos com prefixo `#` (ex: `# Racial Traits.md`) são índices introdutórios; sem prefixo são conteúdo.
- **Provider ativo é Minimax** — não implementar adapters novos (Claude/Gemini/OpenAI/GLM) a menos que o usuário peça explicitamente. Foco no Minimax primeiro.
- **D&D 5e, PHB only** no MVP. Níveis 1-5 no MVP (estendido a 1-20 pelas Fases 25f/25g). Sem multiclasse, sem feats, sem classes/raças/magias fora do PHB.
- **Idioma do produto**: pt-BR (interface, narração, mensagens). Código/identificadores em inglês.
- **Tarefas são rastreadas** via TaskList. Ao começar uma fase, marcá-la `in_progress`; ao terminar, `completed`. Criar tasks pra qualquer trabalho com 3+ passos.
- **Aprovar antes de ações destrutivas** (deletar/sobrescrever saves, rodar comandos perigosos).
- **Style**: line-length 100, target Python 3.11, ruff como linter, pytest para testes.

## Onde estamos

- ✅ Fase 0 — Project skeleton
- ✅ Fase 1 — LLM provider abstraction (Minimax + thinking strip)
- ✅ Fase 2 — State models (Character, GameState, Action, etc — 26 testes)
- ✅ Fase 3 — Rules engine core (3a dice ✅, 3b combat ✅ — 56 testes)
- ✅ Fase 4 — PHB loader (9 raças, 12 classes, 290 magias, 37 armas, 13 armaduras, 15 condições — 75 testes)
- ✅ Fase 5 — Character creation (CharacterBuilder fluent + spell selection: 62 testes)
- ✅ Fase 6 — DM Agent and narrative loop (DM_SYSTEM_PROMPT, DMAgent, parser, process_player_action: 48 testes)
- ✅ Fase 7 — Combat system (CombatEngine orquestrador: initiative/turnos, attack/dash/dodge/help/hide/search/ready, death_save, end_combat, validation: 38 testes + 6 integração com narrative loop)
- ✅ Fase 8 — AI Companions (CompanionAgent, roster com 4 personagens pré-definidos, run_companion_turn, parser, prompt personalizado: 32 testes)
- ✅ Fase 9 — Persistence (save_state/load_state atômico com _meta block, slugify, list_saves, delete_save: 30 testes)
- ⏭ Fase 10 — Remaining LLM providers (pulada a pedido do usuário — provider ativo é Minimax)
- ✅ Fase 11 — Polish (GameApp REPL com meta-comandos /help /save /load /list /status /quit, Rich rendering, character creation wizard, setup_new_game, auto-save cadence, integração com main.py via Click, 26 testes de integração)
- ✅ Fase 12 — Conditions engine (PHB 14 conditions + 2 tactical, exhaustion 6-level, immunity > resistance > vulnerability, applied via combat/damage/conditions: 83 testes)
- ✅ Fase 13 — Adventuring (short rest com hit dice, long rest com clears, falling 1d6/10ft cap 20d6, suffocation 1+CON mod rounds: 25 testes)
- ✅ Fase 14 — Languages catalog (PHB 16 standard + 6 exotic, exposto via get_language/get_all_languages: 8 testes)
- ✅ Fase 15 — Poisons, Traps, Diseases (PHB parser com DC/damage/conditions, apply_poison/trigger_trap/apply_disease com saving_throw, tick_effects no round: 31 testes)
- ✅ Fase 16 — Cover and Opportunity Attack (cover_ac_bonus/dex_save_bonus, três níveis + total, attack_roll aplica cover, OPPORTUNITY_ATTACK handler com reaction_used: 12 testes)
- ✅ Fase 17 — ASI + Inspiration (ASI levels 4/8/12/16/19, +2/+1 split com cap 20, inspiration no-stockpile, spend → pending_advantage consumido por attack_roll e saving_throw: 38 testes + 4 integração)
- ✅ Fase 18 — Complete spellcasting (slots com upcast, prepare/unprepare, known-casters, concentration com CON save vs max(10, dmg/2), ritual casting, cast_spell + handler em CombatEngine, concentração quebra ao tomar dano em attack/OA: 56 testes)
- 🐛 Fix: regex do PHB spell loader tolerava "(ritual)" no header (era 0 rituais carregados → agora ~30)
- ✅ Fase 19 — Barbarian Rage (is_raging/rages_used/rages_max em Character, engine/rage.py com can_rage/enter_rage/end_rage/tick_rage_duration/recover_rages, dano bonus STR melee +2/+3/+4 por nível, resistência bludgeoning/piercing/slashing, vantagem em STR save, ActionType.RAGE handler, auto-end em incapacitação: 63 testes)
- ✅ Fase 20 — Sneak Attack (Rogue), Divine Smite (Paladin), Extra Attack (martial 0/1/2/3 by level tracked em CombatEngine._attacks_remaining), Fighting Style (archery/dueling/defense/GWF/protection/TWF, integrated em attack/damage), Cunning Action (Rogue L2 dash/disengage/hide como bonus: 113 testes somando as 5 sub-fases)
- ✅ Fase 21 — Resource Pools (engine/resources.py consolidando Ki/Sorcery/Lay on Hands/Second Wind/Action Surge/Channel Divinity/Bardic Inspiration com aggregate short_rest_recovery/long_rest_recovery, Sorcery slot↔points conversion PHB p.101: 30 testes); Arcane Recovery (engine/arcane_recovery.py: Wizard L1, cap ceil(L/2), no slots above 5: 15 testes)
- ✅ Fase 22 — CombatEngine handlers (SECOND_WIND, ACTION_SURGE, LAY_ON_HANDS, CHANNEL_DIVINITY, BARDIC_INSPIRATION, FLURRY_OF_BLOWS, STUNNING_STRIKE, UNCANNY_DODGE, RECKLESS_ATTACK, INDOMITABLE — todos com validação de classe + flag de pool, registered em _ACTION_HANDLERS: 37 testes)
- ✅ Fase 23 — Passive defenses (engine/defenses.py: Unarmored Defense Barb DEX+CON/Monk DEX+WIS, Danger Sense (adv DEX save), Brutal Critical (+1/+2/+3 dice on crit, integrated em damage_roll), Evasion (Rogue/Monk L7), Aura of Protection (Paladin L6/18, +CHA mod to saves for self+allies in 10/30 ft, integrated em saving_throw): 29 testes
- ✅ Fase 24 — Specialists (engine/specialists.py: Wild Shape Druid L2+ com CR cap 1/4→1/2→1 e form catalog, Favored Enemy (Ranger), Eldritch Invocations (Warlock), Divine Sense, Destroy Undead CR cap): 25 testes
- ✅ Fase 25a — Monsters loader (Monster/MonsterAction/MonsterTrait + enums em phb/models.py; load_monsters/parse_monster_file em phb/loader.py com parser próprio para o formato `**Field** valor`; get_monster/get_monsters com cache; monster_to_npc adapter; 80 testes)
- ✅ Fase 25b — Subclasses selection + features (get_subclass/get_subclasses_for/get_all_subclasses em phb/lookup.py; Character.subclass_features + character/level_up.py com apply_subclass_features / list_subclass_features / features_gained_at_level / has_subclass_features; CharacterBuilder chama apply_subclass_features no build; wizard em cli/character_flow.py com _prompt_subclass entre classe e background; 27 testes)
- ✅ Fase 25c — Backgrounds + Tools + Gear loaders (Background/PHBTool/PHBEquipmentPack models + ToolCategory/GearCategory enums; load_backgrounds com 13 backgrounds PHB, load_tools com 36 tools (artisan/gaming/musical/kit), load_gear com 132 itens, load_packs com 7 packs; parse_weight_lb suporta frações Unicode; CharacterBuilder._build_proficiencies auto-aplica skill/tool/lang do background, with_starting_pack popula inventário; 54 testes)
- ✅ Fase 25d — Magic Items loader + wire-in (MagicItem/Rarity/MagicItemType em phb/models.py; load_magic_items em phb/loader.py com parser de tagline que cobre rarity varies/multi-rarity/attunement clauses (incluindo "by a paladin"); get_magic_item/get_magic_items/roll_magic_item em lookup.py; Item.magic_bonus+requires_attunement+rarity em state/models.py; engine/combat.py: +1/+2/+3 de weapon em attack_roll+damage_roll, magic_armor_bonus somado de armor/shield em effective_ac; 31 testes)
- ✅ Fase 25e — Movement + Mounts/Vehicles (Mount/Vehicle models + VehicleType enum; load_mounts com 8 mounts + load_vehicles com 16 (10 land + 6 water), parse de mph com fração Unicode (1½, 2½); engine/movement.py com climb_check/swim_check (Athletics) + grapple/shove (contested Athletics) + forced_disadvantage_swim para plate; Character.is_mounted+mount_id, NPC.is_mount+rider_id+is_vehicle+vehicle_type; CombatEngine handlers MOUNT/DISMOUNT; 37 testes)
- ✅ Fase 25f — Leveling L6-L11 (XP_THRESHOLDS L1-L20 + level_for_xp/xp_to_next_level/proficiency_bonus_for; level_up com HP+prof+extra_attacks+hit_dice_remaining; apply_class_features em character/level_up.py com gates Barbarian L2/L7, Paladin L6/L10, Rogue L2/L5/L7, Monk L7, Fighter L9; Character.has_feral_instinct+aura_of_courage_active flags; /level-up meta-command com narration; 69 testes)
- ✅ Fase 25h — REPL polish + **fix crítico do companion turn integration** (CombatEngine.next_actor_id/is_player_turn/is_companion_turn; GameApp._run_companion_cycle itera initiative após o player até voltar a ele ou combate acabar; NarrativeResult.companion_results; META_COMMANDS expandido com /encounter /look /inventory /conditions /spells /level-up; render_inventory/render_conditions/render_spellbook em cli/rendering.py; 36 testes)
- ✅ Fase 25g — Leveling L12–L20 + class capstones (spell slot tables L1–L20 completas para full casters (Wizard/Cleric/Druid/Bard/Sorcerer PHB p.113) e half casters (Paladin/Ranger); Warlock Pact Magic 1/2/3/4 slots L1/L2/L11/L17 todos no mesmo slot level; cantrips known L1–L20 com thresholds 1/4/10; spells known/prepared caps (Bard 22, Sorcerer 15, Wizard spellbook 44); 9th level spell access em L17+; capstones por classe em L20: Primal Champion Barbarian (+4 STR/CON max 24, +2 weapon damage), Signature Spells Wizard (2 magias ≤3º, sempre preparadas, 1x/short rest free cast), Arcane Apotheosis Sorcerer (cap 20 sorcery points), Archdruid Druid (cast em Wild Shape), Perfect Self Monk (4 ki → recover all), Foe Slayer Ranger (+WIS atk/dmg favored enemy 1x/turn), Stroke of Luck Rogue (1x/short rest), Eldritch Master Warlock (refuel pact slots 1x/long rest), Divine Intervention Improvement Cleric; Mystic Arcanum Warlock (6º L11, 7º L13, 8º L15, 9º L17) com learn/cast/reset; engine/class_features.py novo módulo runtime; brutal_critical_dice 1/2/3 em L9/13/17; Character flags novos (has_primal_champion/has_arcane_apotheosis/has_signature_spells/has_archdruid/has_perfect_self/has_foe_slayer/has_stroke_of_luck/has_eldritch_master/has_divine_intervention_improvement) + mystic_arcanum_known/uses + signature_spell_names/uses_remaining; 118 testes)
- ✅ Fase 26a — Web FastAPI skeleton + auth (signup/login/me + bcrypt+JWT) + Postgres User/Save ORM + Redis-backed SessionManager + console UI HTML/CSS/JS + auth screen + lobby screen + game screen com `/help /save /load /list /status /quit`, integração com StaticFiles mount e lifespan startup; FRONTEND_URL (comma-separated) controla CORS; JWT_SECRET ≥32 chars obrigatório; 30 testes web
- ❌ Fase 26b — **Removida** a pedido do usuário. `web/sse.py` + rotas `POST /api/sessions/{sid}/stream` e `/opening/stream` + toggle frontend + funções `sendInputStream`/`playOpeningStream` + helper `iter_stream_with_usage` em `llm/usage.py` + métodos `DMAgent.stream_with_usage`/`stream_opening_with_usage` + `iter_stream_with_usage` em `openai_compatible.py` + `_LegacyProvider.stream` stub nos testes foram deletados; `LLMProvider` Protocol perdeu `stream()`; `chat_with_usage`/`UsageReport` preservados (usados pelo billing da Fase 30). Mensagens agora sempre chegam inteiras via `POST /input`.
- ✅ Fase 26c — Character creation wizard no browser (web/routes_setup.py com GET /api/character-options retornando catalog de raças+subraces, classes+subclasses+skill_options+num_skill_choices+is_spellcaster, backgrounds, alignments, levels, stats_methods, companions; POST /api/sessions/with-character aceita spec + constrói Character via CharacterBuilder + companheiros via COMPANION_FACTORIES + cria GameState+sessão+slug; PlayerCharacterSpec aceita stats_method standard_array/roll/point_buy/manual com validação de alignment+level+race+class+companion; wizard HTML/CSS/JS de 11 passos com progress dots (name → race → class → subclass → background → alignment → level → stats → skills → companions → confirm) usando choices clicáveis, subrace dropdown, skill checkboxes respeitando num_skill_choices da classe, confirmação visual; integração no lobby via botão "Criar novo personagem (wizard)"; auto-save na finalização; 14 testes wizard)
- ✅ Fase 26d — Deploy (Dockerfile python:3.11-slim single-stage com user não-root, HEALTHCHECK, EXPOSE 4004, uvicorn --factory; .dockerignore excluindo .env, .git, tests/, caches; docker-compose.yml com serviço auto-dm exposto em 127.0.0.1:4004:4004 + env passthrough via ${VAR:?error} pra JWT_SECRET/AUTO_DM_API_KEY + healthcheck; LLMConfig.from_env(prefix="AUTO_DM_") lê provider/api_key/model/base_url/temperature/max_tokens/thinking do ambiente; web/server.py::_default_provider_factory agora carrega MinimaxProvider do env sem precisar passar factory explicitamente; testes cobrem from_env happy/invalid/missing + factory errors; DEPLOY.md cobrindo prereqs Debian+Docker+Postgres+Redis já rodando, .env com DATABASE_URL/REDIS_URL/JWT_SECRET/FRONTEND_URL/AUTO_DM_*, setup do schema idempotente, nginx reverse proxy com proxy_buffering off + chunked_transfer_encoding pra SSE, TLS via certbot, opções Vercel vs backend-serving-static, backups Postgres+Redis, troubleshooting table, hardening checklist; 6 testes LLMConfig.from_env)
- ✅ Fase 26e — Invite-code gate (config.py: invite_code Optional[str] via env INVITE_CODE; routes_auth.py SignupRequest.invite_code + signup endpoint valida com hmac.compare_digest antes de criar user; 403 com mensagem genérica "missing ou wrong" pra não vazar qual falhou; login não afetado pelo gate; testes cobrem open signup, ignored field, gated sem code/wrong code/correct code, paridade de mensagens missing↔wrong, login pós-gate, sanity de compare_digest; frontend ganha input auth-invite + envia invite_code só quando preenchido; 8 testes)
- ✅ Fase 27 — Companion pool expansion + synergy roll (`roster.py` com 12 factories cobrindo todas as classes PHB: thorgrim/lyra/mira/vex + garrick (paladino humano, Devotion, sem spellcasting em L1), brom (bárbaro meio-orc, Berserker), kael (mago elfo da floresta, Evocation, spellbook 6), sage (sorcerer meio-elfo, Draconic Bloodline), maren (monja humana, Open Hand), eldra (druida gnoma da floresta, Land), tobias (bardo dragonborn, Lore), dax (warlock halfling stout, Fiend, pact slot L1); todas as personalidades + traits/ideals/bonds/flaws únicos em pt-BR; `COMPANION_BLURBS` centralizado em `roster.py` removendo duplicação em `cli/setup.py` e `web/routes_setup.py`; novo módulo `auto_dm.companions.selection` com `ROLE_TAGS`/`_CLASS_ROLES`/`SYNERGY_BIAS`/`SAME_CLASS_WEIGHT` e função `roll_party_candidates(player, k=4, *, rng=None)` usando weighted random sampling sem reposição com penalidade same-class 0.3 + synergy boost por tag ausente (healer 2.0, tank 1.5) + healer-guarantee retry até 50x quando player não tem role healer; CLI reordena `setup_new_game` pra criar player antes de promptar companions e usa `_prompt_companions(inp, out, player)` listando apenas os 4 rolados; web ganha endpoint `POST /api/companions/roll` que recebe `{class, subclass}` e retorna 4 candidates via stub Character; wizard `openWizard` deixa `wizardState.companions` vazio e `renderWizardCompanions` faz lazy fetch dos candidates antes de renderizar checkboxes; 12 testes por companion + 14 testes em `test_companions_selection.py` cobrindo deterministicidade, deduplicação, same-class avoidance, healer guarantee, role coverage + 3 testes novos no wizard endpoint; bugfix incidental: `getattr(ch, "class", None)` → `getattr(ch, "class_", None)` em `routes_setup.py` (Pydantic alias; `class` é palavra reservada) — `class_` retornava vazio no catalog; ~32 testes novos)
- ✅ Fase 28 — Input-blocking + busy feedback (CLI + web). `src/auto_dm/main.py::_run_repl` envolve `game.process_input` em `Console.status("dots", "Mestre está pensando...")` para linhas não-meta (meta-comandos `/...` continuam instantâneos — detectados por prefixo `/` antes do `with`); `src/auto_dm/web/static/app.js` ganha flag module-level `let busy` + helpers `lockUi()` / `unlockUi()` / `showTyping()` / `hideTyping()` que gateiam `#cmd`, `#send-btn`, `#stream-toggle` e protegem o keydown handler do Enter; `try/finally` em `sendInput` garante unlock em qualquer code path (success, HTTPException, network failure, malformed response); typing indicator = `<div class="typing-indicator">` com 3 dots animados via CSS `@keyframes typing-bounce` (stagger 0.15s) appended em `#output` abaixo da linha do jogador, removido pelo `hideTyping()` no `finally`; stream path perde o `sendBtn.disabled` inline (agora via `lockUi()` herdado do `sendInput`); `style.css` ganha `input:disabled` (cursor not-allowed, bg `#1a1e28`) e o bloco `.typing-indicator` (mantém `button:disabled` separado — diferentes tratamentos visuais); sem mudanças server-side (per-session lock deferido — fora de escopo desta fase, documentado como limitação aceitável pra multi-tab race); wizard, lobby e auth screens não tocados; **1584 testes passando** (0 novos automatizados — sem JS test runner; verificação manual via checklist na plan).

**1573 testes passando** (1584 anteriores − 7 SSE removidos + 3 streaming-related em `test_provider_usage` + 1 em `test_dm_agent`).

- ✅ Fase 29 — User roles (admin/user) + recursos de admin. `models.py::UserRole` enum (`USER`/`ADMIN`) + `User.role` (default `'user'`, `server_default='user'`); migração idempotente `server.py::_ensure_user_role` (mesmo padrão de `_ensure_save_columns`) roda no `lifespan`; seed do admin único no startup via `server.py::_seed_admin(settings)` — cria `User(role=admin)` com `ADMIN_USERNAME` (default `admin`) + `ADMIN_PASSWORD` se definida (sem senha, loga WARNING e pula), idempotente (não duplica); `config.py::Settings.admin_username/admin_password`; `auth.py::require_admin` dependency (403 se `role != admin`); `UserOut.role` exposto em signup/login/me; `signup` sempre cria `role=user` (não aceita role no body — sem escalonamento); `routes_game.py::POST /api/sessions` (Criar jogo vazio) agora `Depends(require_admin)`; novo `routes_admin.py` (router `/api/admin`, todas via `require_admin`): `GET /saves?archived=` lista saves de **todos** usuários com username (joinedload Save.user), `GET /saves/{user_id}/{slug}` retorna snapshot read-only `state`+`narrative_log` (sem criar sessão, sem LLM), `DELETE /saves/{user_id}/{slug}` exclui save de qualquer usuário (archived ou não); frontend `app.js::isAdmin()` gateia UI — lobby admin busca `/api/admin/saves` com tag `@username` por linha + botão "Excluir" (danger, confirm) por save, "Visualizar" abre jogo **read-only** (`viewSaveReadOnly` → `enterGame({readOnly, narrativeLog})` que renderiza `renderNarrativeLog` desabilitando `#cmd`/`#send-btn` e respeitado por `unlockUi`/`sendInput`/`returnToLobby`); "Opções avançadas"/"Criar jogo vazio" ocultos para `user`; header mostra `(admin)`; `index.html`/`style.css` cache bump v32 + `.owner`/`button.danger`. **88 testes web passando** (69 anteriores + 19 novos em `test_admin_roles.py` cobrindo role no UserOut, signup anti-escalation, `/api/sessions` 403 p/ user, rotas admin 401/403/200, listagem cross-user com dono, inspect narrative_log read-only em archived/não, delete cross-user, seed admin create/skip/idempotent, `_ensure_user_role` idempotente); 6 testes existentes em `test_routes_game.py` migrados de `auth_token` → `admin_token` (criação de sessão agora exige admin).
- ✅ Fase 30 — Painel admin: gestão de usuários, limites de uso, custo e atividade. **Captura real de usage**: novo `llm/usage.py` com `UsageReport` (prompt/completion/total/provider/model/source `"api"|"fallback"`) + helper `chat_with_usage` (prefere método nativo do provider, senão fallback chars//3 marcado `source="fallback"`); `openai_compatible.py` ganha `chat_with_usage` (lê `response.usage`) e `chat()` vira wrapper — Protocol base **intacto** (CLI não quebra); `DMResponse`/`CompanionDecision` ganham campo `usage`, `NarrativeResult.usages` acumula 1-2 chamadas DM (companion turn fora do path web). **Modelo de dados**: `User` ganha `daily_token_limit`/`daily_minutes_limit` (NULL→default global), `unlimited`, `active`, `disabled_reason`; novas tabelas `usage_events` (id/user_id CASCADE/session_id/endpoint/kind/provider/model/source/prompt/completion/total_tokens/cost_usd NUMERIC(12,8)/created_at) e `activity_log` (id/user_id CASCADE/event_type/meta JSON/created_at) + enums `UsageKind`/`ActivityType`; `config.py` defaults `default_daily_token_limit=200_000`, `default_daily_minutes_limit=120`, `token_price_per_1k_input_usd=0.001`, `token_price_per_1k_output_usd=0.002`. **Helpers**: `web/usage.py` (dialect-aware: `compute_cost`, `usage_today`, `minutes_today` via `date_trunc`/`strftime`, `cost_this_month`, `usage_by_day`, `persist_usage_events`), `web/limits.py::check_quota` (None se ok; isento se `unlimited` ou role admin; 429-detail `{detail,used,limit,unit,reset_at}` com reset à meia-noite UTC), `web/activity.py::log_activity` (best-effort). **Migrações idempotentes** `server.py::_ensure_user_limits` (5 colunas em `users`, defaults dialect-aware) + `_ensure_usage_tables` (`CREATE TABLE IF NOT EXISTS` JSONB/TEXT) rodam no `lifespan` após `_ensure_user_role`. **Endpoints admin** (`routes_admin.py`, todos `require_admin`): `GET/POST /api/admin/users`, `GET/PATCH/DELETE /api/admin/users/{id}`, `POST .../reset-password`, `GET .../activity`, `GET .../usage?days=`, `GET /api/admin/usage/summary` (custo/tokens do mês, top 5, contagem ativos/desativados); proteções: não desativar/demover/excluir a si nem o **último admin ativo** (409). **Auth hooks**: `current_user` barra `active=False` (403 "Conta desativada" — mata sessões zumbis); `login` barragem genérica 401 p/ inativo (anti-enumeration) + `ActivityLog(login)`; `signup` loga `signup`. **Enforcement**: `routes_game.py::session_input` chama `check_quota` **antes** do LLM (429 + `ActivityLog(limit_blocked)`) e persiste `UsageEvent` depois (1 por chamada DM). **Frontend**: nova `#admin-panel-screen` (`index.html`) com dashboard (cards custo/tokens/ativos), tabela de usuários (status tags ativo/desativado/ilimitado, tokens hoje/limite, custo mês), modais Criar/Editar/Resetar senha, drawer Atividade+Uso; botão "Painel admin" no lobby (`isAdmin()`); `api()` anexa `err.status` p/ distinguir 429 → mensagem pt-BR "Limite diário atingido..." em `#output`; cache bump v33; CSS `.admin-table`/`.admin-card`/`.modal`/`.tag`. **1640 testes passando** (1608 anteriores + 8 `test_provider_usage` + 16 `test_admin_users` + 8 `test_usage_limits` cobrindo captura API/fallback, propagação agent→narrative, `usage_today`/`check_quota` unit, 429 no `/input`, persistência `usage_events`, admin isento, override `unlimited`, CRUD users 201/409/404, proteções self/último-admin, reset-password + login, soft-disable 401 genérico + `current_user` 403, activity log login).
- 🐛 Fix: narrative_log ao carregar save (frontend) — `loadSaveAsSession` em `app.js` recebia `res.state` (que contém `narrative_log` populado pelo `model_dump_json` do Pydantic) mas chamava `enterGame()` sem repassar; `enterGame` só renderizava o log no branch `readOnly` (admin). Resultado: jogador só via "Sessão iniciada" e perdia o histórico. Fix: `loadSaveAsSession` agora passa `narrativeLog: (res.state && res.state.narrative_log) || []`; `enterGame` adiciona `renderNarrativeLog(narrativeLog)` no caminho não-readOnly (após a linha de sistema, gateado por `narrativeLog.length` pra não duplicar abertura em jogos novos). Index `?v=39` → `?v=40` pra invalidar cache. Sem mudança server-side — persistência/restore via `state.model_dump_json`/`model_validate_json` já cobriam `narrative_log` corretamente (admin read-only sempre funcionava); a inconsistência era só na renderização do load normal vs. `viewSaveReadOnly`.
- ✅ Fase 31 — Narração por campanha (curto / medio / longo). `GameState.narration_length: Literal["curto","medio","longo"] = "longo"` (default longo = comportamento original, preserva saves antigos via Pydantic); `NARRATION_LENGTH_BUDGETS` em `agents/prompts.py` com diretiva por nível preservando tensão/exploração subordinados ao teto geral; `get_narration_directive` / `get_followup_max_sentences` injetados em `DMAgent._build_messages` + `_narrate_action_result`; prompt CLI `_prompt_narration_length` em `cli/setup.py` (1/2/3 ou keyword, fallback longo); wizard web ganha `<select id="wz-narration-length">` no passo 1 + `narration_lengths` no catalog `/api/character-options` + `narration_length: Literal[...] = "longo"` em `WithCharacterRequest`; `index.html` + `app.js` cache bump `?v=40` → `?v=41`. **1693 testes passando** (1640 anteriores + 51 narração cobrindo state default/roundtrip/back-compat, prompt helpers (incl. fallback desconhecido → longo), injeção em `DMAgent._build_messages` para cada nível, follow-up usando budget certo por nível, DM_SYSTEM_PROMPT referenciando seção + preservando bullet original de tensão/exploração, CLI helper cobrindo 1/2/3/keyword/empty/invalid/out-of-range, wizard catalog + POST curto/medio/default-longo + 422 em "epico", persistência round-trip por nível + save antigo sem campo carrega como longo). 5 falhas pré-existentes (Phase 26b deixou `test_opening_stream_*` órfãs; `test_signup_invite*` tem cache de env — não relacionadas a esta fase).
- ✅ Fase 32 — Cenário inicial opcional na criação de campanha. `GameState.initial_scenario: str = ""` (default vazio = LLM decide livremente; preserva saves antigos via Pydantic); bloco "Cenário inicial definido pelo jogador" injetado em `build_dm_context_block` **apenas quando `state.initial_scenario` não-vazio E `narrative_log` vazio** (depois da abertura, o cenário já está no diário e repetir seria desperdício de tokens); bullet 6 adicionado na seção "Abertura de campanha" do `DM_SYSTEM_PROMPT` instruindo o DM a usar o cenário como base autoritativa quando presente; CLI ganha `_prompt_initial_scenario` multi-linha em `cli/setup.py` (linha em branco encerra input, primeira linha vazia = pulou) + `_summarize_scenario` para o painel Resumo; wizard web ganha `<textarea id="wz-initial-scenario" maxlength="2000">` no passo 1 com hint "Deixe em branco para o mestre decidir livremente" + `WithCharacterRequest.initial_scenario: Optional[str] = Field(default=None, max_length=2000)`; admin "jogo vazio" ganha `<textarea id="new-scenario">` na seção avançada do lobby que vira `initial_scenario` no state; `index.html` + `app.js` cache bump `?v=41` → `?v=42`. **1724 testes passando** (1693 anteriores + 31 novos: 6 state default/roundtrip/multilinha/empty-string/old-save/in-model-dump-json, 3 persistence roundtrip+empty+backward-compat, 4 DM injection presente-no-opening/omitido-após-log/omitido-quando-vazio/system-prompt-contém-regra, 12 CLI helper multi-linha/EOF/whitespace + summarize boundary, 6 wizard POST persiste/omite/explícito-vazio/oversized-422/no-limite-aceito/multilinha). 5 falhas pré-existentes (Phase 26b deixou `test_opening_stream_*` órfãs; `test_signup_invite*` tem cache de env — não relacionadas a esta fase).
- ✅ Fase 33 — Memória de longo prazo via sumarização periódica (resolve a amnésia do DM). **Schema**: `GameState.summary_enabled`/`summary_every_n_entries=20`/`summary_char_threshold=12_000`/`last_summarized_at_index`/`last_summary_attempt_at_index` (5 campos novos, defaults sensatos; saves antigos carregam sem migração). **Módulo novo** `agents/summarizer.py`: `SUMMARIZER_SYSTEM_PROMPT` (pt-BR, regras de preservação), `NO_SUMMARY_SENTINEL`, `should_summarize(state)` (entry-count OU char threshold, cooldown de 3 entradas via `last_summary_attempt_at_index`, kill switch via `summary_enabled`), `_parse_summary` rejeita empty/sentinel/<50 chars/markdown-only, `NarrativeSummarizer.summarize` condensa `narrative_log[:-6]` via `chat_with_usage` (provider nativo ou fallback chars//3), `apply_summary` append + dedup trivial, `summarize_once` end-of-turn helper — **never raises**, loga warning + avança apenas `last_summary_attempt_at_index` em falha. **Injeção de prompt**: nova seção `## Resumo de eventos anteriores` em `build_dm_context_block` (entre `## Estado do mundo` e `## Party` — world-level, não diário) injetando apenas `summary_history[-1]`; entradas mais antigas ficam em disco para inspeção admin/replay. **Hooks**: `process_player_action` e `generate_opening` recebem kwarg `summarizer` e chamam `summarize_once` no fim do turno (path `narrative.py::_maybe_summarize` taggeia `usage.kind="summarizer"` antes de apender em `result.usages`); `run_companion_turn` faz o mesmo (custo do summarizer companion-turn é descartado, não billed). **CLI**: `/summary` (status) | `/summary on|off` (toggle) | `/summary status` (config detalhada) | `/summary force` (zera cursor + roda + autosalva) — todas no `META_COMMANDS`; novo `GameApp._summarizer` field construído em `initialize()` e reconstruído em `_load()`. **Web**: `UsageKind.SUMMARIZER = "summarizer"` em `web/models.py`; `routes_game.py::session_input` agora bucketa `result.usages` por `kind` (vazio→`"player"` por back-compat) e persiste `kind="summarizer"` em `usage_events` separado da quota do jogador. **Desvio documentado do plano**: `UsageReport` ganhou `kind: str = ""` em vez de mudar `result.usages` para `list[(UsageReport, UsageKind)]` — evita ciclo agents→web e mantém o path CLI funcionando. **1769 testes passando** (1724 anteriores + 45 novos em `tests/test_summarizer.py`: TestSchema, TestShouldSummarize, TestParseSummary, TestApplySummary, TestNarrativeSummarizer, TestSummarizeOnce, TestBuildDMContextBlockInjection, TestProcessPlayerActionHook, TestPersistence, TestCLISummaryCommand cobrindo defaults schema, round-trip Pydantic/save, predicate entry-count + char + cooldown + kill, parser empty/sentinel/markdown/short/valid/strip-headers, apply_summary append/dedup/reject, summarize calls LLM com slice correto + summary prévio + rejection criteria, summarize_once noop+disabled+exception+attempt-only+success+parser-rejection, build_dm_context_block section ausente/em-ordem/only-once, process_player_action appends `kind="summarizer"` no result.usages, save/load roundtrip campos novos, CLI `/summary status/on/off/force/force-curto`). 5 falhas pré-existentes (mesmas do Phase 32 — não relacionadas a esta fase).
- 🗑️ Fase 34 — **Remoção do CLI** (foco 100% no web). Deletados `src/auto_dm/cli/` (app.py, setup.py, character_flow.py, rendering.py, `__init__.py`) e `src/auto_dm/main.py`; removido o entry point `auto-dm = "auto_dm.main:main"` e as deps `click`/`rich` do `pyproject.toml` (usadas só pelo CLI). CLI era folha arquitetural pura — nada em `web/`/`agents/`/`engine/` importava dele, então o corte foi limpo. Testes: deletados os 100% CLI (`test_cli.py`, `test_cli_phase_25h.py`, `test_character_flow.py`, `test_cli_setup_scenario.py`, `test_cli_setup_narration.py`); cirurgia nos que pegavam algo do CLI de carona (`test_spell_slots_phase_25g.py`/`test_progression_phase_25f.py` perderam os testes de `META_COMMANDS`; `test_subclass_phase_25b.py` perdeu `TestPromptSubclass`; `test_summarizer.py` perdeu `TestCLISummaryCommand` + imports `io`/`redirect_stdout` órfãos). Docs limpos de CLI (README/CLAUDE/SPEC/PLAN). O módulo `persistence/` (save/load JSON) foi preservado — ainda usado por testes e helpers. A lógica de setup compartilhada (CharacterBuilder, roster, companion selection) já vivia fora do CLI e é consumida direto pelo `web/routes_setup.py`. Meta-comandos in-game (`/help /save /load /list /status /quit`) continuam via web. **1656 testes passando** (antes ~1769; a queda são os ~108 testes exclusivos de CLI removidos) + as mesmas 5 falhas pré-existentes (`test_opening_stream_*` órfãs da Fase 26b; `test_signup_invite*` com cache de env) — 0 falhas novas introduzidas.
- 🐛 Fix: narração de abertura truncada no meio da palavra (`finish_reason="length"`). Causa: `max_tokens` default de 2048 em todos os pontos (LLMConfig, `from_env`, `config.py`, `${AUTO_DM_MAX_TOKENS:-2048}` nos dois compose) **compartilhado com os tokens de thinking do MiniMax** — o M3 com `thinking: adaptive` emite `<think>...</think>` inline no mesmo `content`, então o raciocínio consumia boa parte do budget antes da narração visível; a abertura "longo" (cinco sentidos + apresentar companheiros + gancho + bloco `action`) estourava o resto e a API cortava silenciosamente (ninguém checava `finish_reason`; o bloco `action` do final também se perdia → `move` de abertura não registrado). A Fase 31 não causou — a diretiva "longo" só deixou as respostas tipicamente maiores, expondo o limite latente. Fix: default 8192 em `llm/base.py` (dataclass + `from_env` + fallback de ValueError), `config.py`, `docker-compose.yml` e `docker-compose.dev.yml` (cap não gera custo se não usado); `OpenAICompatibleProvider.chat_with_usage` agora loga `WARNING` quando `finish_reason == "length"` (aponta pra `AUTO_DM_MAX_TOKENS`) — truncamento nunca mais é silencioso. Se `AUTO_DM_MAX_TOKENS` estiver **explícito no `.env` do deploy**, o valor de lá vence o default do compose e precisa ser subido manualmente. **Escape hatch do admin**: `AUTO_DM_MAX_TOKENS=0` (ou negativo) = sem limite — `_request_kwargs` omite o campo `max_tokens` da request e vale o teto de output do próprio modelo (útil se algum dia 8192 cortar de novo; ativar exige editar `.env` + recriar o container). O warning de truncamento distingue os dois casos (cap configurado → sugere subir/zerar `AUTO_DM_MAX_TOKENS`; sem cap → aponta o teto do modelo). Testes em `test_provider_usage.py` (warn em `length` capped/uncapped, silêncio em `stop`, `_request_kwargs` inclui cap positivo / omite em 0 e negativo) + `test_llm_config_from_env.py` (defaults 8192, `MAX_TOKENS=0` → 0); suíte completa idêntica ao main limpo (0 regressões).
- ✅ Fase 35 — Sugestão de nomes com IA no wizard (QoL). Botão ✨ ao lado de cada input de nome (campanha/personagem) no passo 1 do wizard gera o campo via LLM pra evitar o "branco" do jogador. **Backend**: `POST /api/suggest-names` em `routes_setup.py` (auth obrigatória) recebe `{kind: "campaign"|"character"|"both"=both, theme?: str≤200}`, monta prompt pt-BR pedindo **JSON estrito** (`{"campaign_name", "character_name"}`), chama `chat_with_usage(factory(), ...)`, parse robusto via `_extract_json_object` (regex `\{.*\}` tolera code-fences/prose) + `_clean_name` (strip aspas/whitespace, cap 80 chars), devolve `SuggestNamesResponse`. Enforce consistente: `check_quota` antes (429 + `ActivityType.LIMIT_BLOCKED`) e `persist_usage_events` depois com **novo** `UsageKind.NAMING = "naming"` (custo separado no painel admin, mas conta na quota diária pois `usage_today` soma todos os kinds); `UsageReport.kind` setado via `dataclasses.replace` (dataclass é frozen). Erros de LLM/parse → 502; key obrigatória ausente → 502; sem provider → 503. **Frontend**: `index.html` envolve cada input num `.input-with-ai` com `<button class="ai-suggest-btn">✨</button>` (title + `.sr-only` p/ acessibilidade); `app.js::suggestWizardName(kind)` posta no endpoint, aplica o valor no input + `wizardState`, spinner `⏳` no botão durante a chamada, `try/finally` restaura o botão, 429 → `LIMIT_REACHED_MSG`, outras falhas em `#wizard-msg`; wiring no init. **CSS**: `.input-with-ai` (flex), `.ai-suggest-btn` (gold `#c89b3c` sobre `#2a2519`, scale no active, disabled state), `.sr-only`. Cache bump `?v=44` → `?v=45`. **16 testes novos** em `tests/web/test_suggest_names.py` (helpers `_clean_name`/`_extract_json_object`, both/campaign/character/default-both, strip aspas, 502 unparseable, 502 key missing, 401 sem auth, 429 quota via monkeypatch check_quota, persiste `UsageEvent` com `kind=naming`, 422 theme oversized). 5 falhas pré-existentes (mesmas — `test_opening_stream_*` órfãs 26b; `test_signup_invite*` cache de env); 0 regressões.
- ✅ Fase 36 — Fichas dos companheiros (espelho da ficha do jogador). Companheiros só apareciam no narrative log como `.entry.companion` — sem painel com HP/CA/atributos/proficiências. **Backend**: novo `GET /api/sessions/{sid}/companions` em `routes_game.py` (auth obrigatória) retorna lista com o mesmo shape do player character (HP atual/max, CA, abilities, proficiências, condições, spellcasting, inventário), filtrando `state.party` em `!is_player` — sem LLM, sem persistência nova, mesmo TTL/semântica do `GET /sessions/{sid}`. Docstring do módulo atualizada pra incluir o endpoint novo. **Frontend**: `#table-tools` ganha `.sheet-stack` com `.char-tabs` (uma tab por personagem: `Jogador` + cada companheiro) e `#sheet-host` que hospeda N `.sheet-view` (apenas `.active` visível). `renderCharacterTools()` agora itera party inteira, ordena player primeiro, gera tab + view por char; `activeSheetId` em module-scope persiste a aba ativa entre chamadas de `/input` mas é resetado em `enterGame()` (sessão nova abre sempre no player). Cada `.sheet-view` mostra o mesmo mirror da ficha do jogador (HP/CA/Prof/Speed + abilities+mods + proficiências + condições como pills coloridas `.cond-pill`); players ganham tag `⚔` + badge "você" no botão; subclasses/backgrounds aparecem no subtítulo quando presentes. Novo helper `escapeHtml` para os campos textuais. Roll-panel inalterado (continua atrelado ao player — roll-check server-side é hard-locked em `state.player_character_id`, deferido pra fase futura). **CSS**: `.char-tabs` (flex), `.char-tab`/`.char-tab.active` (border gold `#c89b3c` no active), `.tab-name`/`.tab-meta`/`.tab-tag`, `.sheet-host { display: contents }` pra preservar padding do `.sheet-panel`, `.sheet-view` hidden default, `.sheet-view.is-player .sheet-head strong::before { content:"⚔" }`, `.cond-pill` (bg `#5c2a2a`, fg `#f0c0c0`). Cache bump `?v=45` → `?v=46`. **7 testes novos** em `tests/web/test_companions_endpoint.py` (auth 401, 404 sessão inexistente, 404 cross-user, filtragem `!is_player`, party só com player = `[]`, shape completo Character no payload, idempotência de leitura) com fixture `autouse` que desabilita o invite-gate local (`monkeypatch.setenv("INVITE_CODE", "")` + `get_settings.cache_clear()`) — `.env` do dev seta a gate. **1539 testes non-web passando** (suite completa não-web sem regressão). As falhas pré-existentes do diretório `tests/web/` (`test_routes_game`/`test_wizard`/`test_suggest_names`/`test_signup_invite` etc.) são amplificadas aqui pelo mesmo `.env` que afeta `test_signup_invite*` desde a Fase 26e — nenhuma regressão introduzida por esta fase; os 7 testes novos do endpoint passam limpos.
- ✅ Fase 37 — Spells + inventário compacto nas fichas (player + cada companheiro). Backend da Fase 36 já serializava `spellcasting`/`inventory` (eram só ignorados pela UI); a Fase 37 os renderiza. **Frontend (`app.js`)**: `renderSheetView()` agora chama `renderSpellsSection(character.spellcasting)` + `renderInventorySection(character.inventory)`. Helpers novos: `renderSpellsSection(sc)` retorna `""` quando `sc` é None (martials pulam a seção inteira), senão emite (a) header **Magia:** CD X · Ataque ±Y · [ability_label] · [Concentrando em *Z*] · [Ritual] usando o campo `attack_bonus` do schema; (b) **Truques:** lista cantrips; (c) **Preparadas (N):** lista `spells_prepared` ou **Conhecidas (N):** lista `spells_known` se prepared estiver vazia; (d) **Livro (N):** lista `spellbook` (Wizard only); (e) **Slots:** linhas `Kº: cur/max` ordenadas por nível via `Number(k)` cast em keys JSON-stringified, filtrando slots 0 e level 0; `renderInventorySection(inventory)` itera itens com `.inv-item`, sufixo `<span class="inv-qty">×N</span>` se `quantity > 1`, e marker `<span class="inv-magic" title="rarity">●</span>` se `rarity` setada e não-`"common"`; retorna `""` quando vazio; `formatSpellName(s)` Title Case `kebab-or-lower` via regex `/(^|[\s-])\w/g` (passa `escapeHtml` por baixo). **CSS**: `.sheet-spells strong` (CD/CD em gold `#c89b3c`), `.sheet-spells em` (concentração regular mas bold), `.sheet-inv strong`, `.inv-item { white-space: nowrap }`, `.inv-qty` (gold, 0.78rem), `.inv-magic` (blue `#6f8cd6`, rarity no title). Cache bump `?v=46` → `?v=47`. **5 testes novos** em `test_companions_endpoint.py` (payload spellcasting completo em caster com `cantrips_known`/`spells_prepared`/`spell_slots_{max}`/`concentration`/`ritual_casting`+ability_label esperado via `ability=="wisdom"`; payload `spellcasting=null` em martial; inventory array com 4 itens incluindo stack ×3 e magic com `rarity="uncommon"`+`magic_bonus=1`; inventory ausente = `[]`; `spell_slots_max` chega com keys isdigit para o cast `Number(k)` no JS). **12 testes passando** no arquivo de companions (7 da Fase 36 + 5 novos da 37), 0 regressões. Mesmo gate-invite autouse continua valendo pros novos testes (não precisam signup).
- ✅ Fase 38 — XP, progressão por party, auto-level-up loop, ASI. Resolve o pedido original "como funciona o XP". **Schema novo**: `GameState.party_xp: int = 0` + `Character.pending_asi: Optional[dict]` (fila do ASI do jogador) + `Character.primal_champion_applied: bool` (idempotência do capstone) + `NPC.xp: Optional[int]` (pré-debitado do PHB CR table via `monster_to_npc`). **Engine** (`engine/progression.py`): `LevelUpReport`/`LevelUpBatch` dataclasses, `current_party_level(state)` derivado do XP (tabela PHB p. 15), `xp_to_next_party_level`, `award_party_xp(state, amount, *, source, rng)`. **Bug fix crítico**: o `level_up` legacy não refrescava `spell_slots`; a Fase 38 corrige com `_refresh_spell_slots` (PHB p. 113 full + half caster tables) e `_apply_class_features` encadeados ao final do level — sem isso, Wizard L5 ainda tinha slots de L1. Também conserta over-walking no `award_party_xp` (delta por-personagem, não global) — anterior fazia um L3 player receber 3 ASIs em um único grant. **ASI split**: `level_up` enfileira `pending_asi={level, choices, resolved:False, primary, secondary}` para `is_player`; auto-resolve para companheiros via `auto_resolve_companion_asi` (heurística deterministic por classe: +2 primário ≤ 18, +1/+1 split em 19/20, fallback +1 CON) + `companion_asi_to_pending` mapeia pra shape da fila. **Combat hook**: `EncounterSummary.xp_awarded` + `EncounterSummary.level_up_batch` populados em `end_combat` (soma XP dos defeated NPCs). **DM context block**: nova seção `## Progressão da party` com `party_xp`/`current_party_level`/`xp_to_next_party_level` + hint "## ASI pendente" quando o jogador tem escolha aberta. **Web API**: `POST /api/sessions/{sid}/award-xp` (auth + validação 1≤amount≤100k + rate limit 10/min via Redis `rate_limit.py::check_rate_limit`, retorna `LevelUpBatch` completo) e `POST /api/sessions/{sid}/resolve-asi` (auth + 404 char não na party + 422 ability desconhecida/split inválido/excede cap/sem fila). **Frontend**: `#levelup-banner` aparece quando DM narra level-up com CTA "Ver detalhes"; `#asi-modal` (radio +2/+1 split, picker para primary/secondary, validação client-side de cap 20) consome `/resolve-asi`; meta-comando `/award-xp [amount]` no clientCommand; helper JS `partyLevelFromXp`/`xpToNextPartyLevel` espelham a tabela PHB; `enterGame` consulta `/companions` pra popular level-up UI. Cache bump `?v=47` → `?v=49`. **78 testes novos** em 4 arquivos: `test_xp_award_phase_38.py` (28: state, party-level math, multi-threshold crossings, spell-slot refresh no level-up, ASI queue player/companion, narrative entries, monster XP no NPC), `test_asi_queue_phase_38.py` (19: 3 paths do auto-resolve, split, fallback CON, primary por classe, idempotência, `resolve_asi_choice` erros, `update_spell_slots_for_level`), `test_combat_xp_phase_38.py` (11: `end_combat` XP aggregation, level-up batch attached, multi-defeated, mixed alive/dead, `EncounterSummary` shape), `test_xp_endpoints_phase_38.py` (20: auth, validação amount, rate-limit 429, response shape, persistência, cross-session isolation, ASI validation). **1675 testes passando** (1597 non-web + 78 novos Phase 38). 0 regressões. Ruff limpo nos arquivos da fase (17 erros pré-existentes em `phb/loader.py`/`engine/movement.py`/`engine/spell_effects.py` — fora de escopo).

- ✅ Fase 39 — Inventário & equipamento na web (loot → equipar → comprar/vender). **Renumeração**: a "segunda onda" do PLAN.md/SPEC.md §12 foi renumerada de 34–39 para **39–43** (34–38 já tinham sido usadas por outras entregas) e a antiga "Fase 34 — Painel em tempo real" foi **removida por decisão do usuário** (fichas das Fases 36/37 reais + jogo turn-based tornam live-polling/ETag desnecessários; seção §12.1 do SPEC marcada REMOVIDA como histórico). **Schema** (state JSON, sem migração de DB — back-compat via defaults Pydantic): `Character.gold_gp: float = 0.0` (float porque preços PHB vão abaixo de 1 gp) + `Character.attuned_items: list[str]`; `NPC.vendor: bool = False` + `NPC.shop_inventory: list[ShopItem]` com novo model `ShopItem{item_id, price_gp, restock_daily}`. **Engine** (`engine/inventory.py`, puro, sem LLM): `InventoryResult` (ok/errors/warnings/ac_before/ac_after/ac_delta property/gold_gp), `compute_armor_class`/`refresh_armor_class` seguindo a convenção do builder (base+DEX capado OU max(10+DEX, unarmored_defense_ac) p/ Barb/Monk, +2 escudo físico; bônus mágicos ficam FORA do stored AC — `attack_roll` soma dinamicamente), `add_item`/`remove_item` com stacking por nome (só itens sem weapon/armor props; remove depletado limpa slots equipados + attunement + refresh AC), `equip_item`/`unequip_item` com validação slot×tipo (armor/main_hand/off_hand/trinkets) + warnings não-bloqueantes (proficiência derivada de `phb.get_class(...).proficiencies` em runtime — o `Proficiencies` do state não tem armor/weapons e adicionar quebraria saves; off-hand não-light; 2H + off-hand ocupado; requer sintonização), `attune_item`/`unattune_item` (cap 3, PHB p. 138), `price_for_item` (value_gp explícito > tabela por raridade common 100/uncommon 500/rare 5k/very_rare 50k/legendary 500k), `resolve_catalog_item` (PHB weapons→armor→gear→magic items, reusando `_weapon_to_item`/`_armor_to_item` do builder), `buy_item` (valida vendor flag/estoque/ouro; erro "ouro insuficiente" vira 402 na web)/`sell_item` (SELL_RATE 0.5). **Web** (`web/routes_inventory.py`, registrado em `server.py`): GET `/api/sessions/{sid}/inventory` (view agrupada por categoria + gold + attuned + equipped + slots), GET `.../shop/{vendor_id}` (catálogo resolvido com `item` payload ou null p/ desconhecidos; 404 NPC ausente, 422 não-vendor), POST `.../inventory/{equip,unequip,drop,attune,unattune,buy,sell}` — todos `current_user` + ownership via `sm.get` (cross-user 404), `character_id` opcional permite gerenciar equipamento de companheiros (mesma sessão/dono), mapeamento de erros 402 ouro / 404 vendor/char / 422 resto, mutações persistem via `sm.save`. **Frontend** (`app.js`/`index.html`/`style.css` v50→v51): `renderInventorySection(character)` agora interativo — linha de Ouro, pills "Equipado" por slot, cada item vira botão que abre `#item-modal` (meta rarity/attunement/dano/CA, descrição, ações Equipar com select de slot inferido por tipo, Desequipar por slot, Sintonizar/Remover sintonia, Soltar 1 com confirm, Vender 1 com preço estimado); `#shop-buttons` na sheet-stack mostra "🛒 Loja: <nome>" por NPC vendor no state; `#shop-modal` overlay com saldo, estoque e Comprar desabilitado sem ouro (402 → mensagem pt-BR); `applyCharacterUpdate` substitui a entrada da party com o Character da resposta e re-renderiza + sincroniza o save via `persistSave`; tudo gateado por `readOnlyMode`. **85 testes novos** (56 engine em `tests/test_inventory_phase_39.py`: model back-compat/roundtrip, AC recompute armored/unarmored/UD/escudo/magic-not-baked, equip/unequip slots + warnings wizard-plate/fighter-plate/greatsword/dagger, stacking/deplete/limpa-slots, attunement cap, preços, buy/sell happy+falhas, resolve catalog; 29 rotas em `tests/web/test_inventory_routes_phase_39.py`: 401/404 auth+cross-user, view agrupada, equip persiste na sessão + 422s + warning de proficiência via companion, drop stack/deplete, attune/4º-422, shop catalog/404/422, buy 200/402/422/404 + quantity, sell 200/422). Suíte completa com `INVITE_CODE=""`: mesmas 5 falhas pré-existentes (26b órfãs + invite env-cache), 0 regressões. Ruff limpo nos arquivos da fase.
- ✅ Fase 40 — Encontros aleatórios + tesouros em viagem. **Desvio deliberado do plano** (SPEC §12.3 original propunha o DM rolar e emitir tags `MEC: encounter <table> <roll>` / `LOOT: hoard <tier> <roll>` / `WEATHER: <table> <roll>` que o engine validaria por regex e confiaria): violaria "mecânica é autoritativa" (princípio #1 do projeto) ao deixar o LLM decidir *quais* números saíram. Redesenhado como **engine-authoritative**: o DM só propõe *que* há viagem e *por quanto tempo* (`params.travel_hours` numa action `move` já existente); o engine sozinho decide encontro/clima/loot e devolve o resultado pro DM narrar — sem `agents/heuristics.py` de intent-detection (também cortado do plano original: um `move` com `travel_hours` estruturado é mais robusto que regex sobre texto livre pt-BR). **Dados novos** (`data/world_tables/`, recurso original curado — não PHB): `encounters/{forest,road}_{day,night}.json` + `dungeon_level_{1,5}.json` (d100, ~5-6 linhas cobrindo 1-100, maioria "nenhum encontro"), `loot/{individual,hoard_low,hoard_mid,hoard_high}.json` (d100, ouro via dice notation + itens de catálogo), `weather.json` (d20, tabela única sem variação por bioma/estação); README.md documenta o schema + simplificações de escopo. Só `individual` está de fato plugada em `resolve_travel` — as tabelas `hoard_*` ficam carregadas/testáveis via `compute_loot()` para uma fase futura de recompensa pós-combate (Fase 40 não amarra loot a derrotar encontro). **PHB loader** (`phb/models.py`+`loader.py`+`lookup.py`+`__init__.py`): `EncounterTable`/`EncounterTableRow`/`EncounterMonsterEntry`, `LootTable`/`LootTableRow`, `WeatherTable`/`WeatherTableRow`; root separado (`get_world_tables_root`/`set_world_tables_root`, default `data/world_tables/`) com cache próprio (sentinela `_weather_table_loaded` porque `None` é um valor de cache válido); `get_encounter_table(s)`/`get_loot_table(s)`/`get_weather_table` seguem a convenção lazy-cache do resto do `phb/`. **Schema** (`state/models.py`, back-compat via defaults Pydantic): `GameState.campaign_seed` (gerado uma vez por campanha, `uuid4().hex[:12]`), `elapsed_game_minutes`, `world_event_cooldown_minutes=30`, `last_world_event_minute`; `NarrativeEntry.world_seed: Optional[str]` (seed usado na rolagem de viagem daquele turno, pra replay/inspeção admin). **Engine** (`engine/world.py`, puro, sem LLM): `roll_encounter(state_manager, biome, time_of_day, *, rng)` rola d100 contra `f"{biome}_{time_of_day}"`, spawna `NPC`s via `monster_to_npc`/`slugify_monster_id` com sufixo anti-colisão de id; `compute_loot(table_id, roll, *, rng)` resolve ouro (dice×multiplier) + nomes de item; `resolve_travel(state_manager, hours, *, combat_engine=None, rng_seed=None, biome="road") -> WorldEventList` orquestra tudo: 1 encontro check por bloco de 4h (gated pelo cooldown, cross-call via `last_world_event_minute` absoluto — não reseta por chamada, então fatiar viagem em pedaços pequenos não rerola mais rápido), 1 clima por chamada, 1 loot check por dia cheio (15% chance, tabela `individual`); encontro interrompe a viagem (`minutes_traveled` para no bloco do encontro, não avança o relógio pelas horas restantes ainda não vividas) e chama `combat_engine.start_combat` se um foi passado; seed determinístico `f"{campaign_seed}:{elapsed_game_minutes}"` (ou override explícito) — mesma seed + mesmo estado = mesmo resultado, verificado em teste dedicado. `time_of_day` interno (`"day"`/`"night"`, chave de lookup da tabela) é distinto do `GameState.time_of_day` exibido ao DM (`"madrugada"/"manhã"/"tarde"/"noite"`, pt-BR, granularidade maior). **Narrativa** (`agents/narrative.py`): `_dispatch_action` ganha um branch novo — `move` com `params.travel_hours` (float > 0, extraído via `_extract_travel_hours` com fallback seguro pra não-numérico/≤0) vai para `_dispatch_travel` em vez do stub de `move` de cena única; monta `ActionResult.mechanical` com `destination`/`travel_hours`/`world_seed`/`elapsed_minutes`/`combat_started`/`world_events` pro follow-up narrar sem inventar; atualiza `state.current_location` quando há destino (gap pré-existente: o `move` stub comum nunca fazia isso — corrigido só no path de viagem, escopo desta fase); `_tag_world_seed` carimba a entrada de follow-up (não a narração inicial) com o seed, condicionado a `follow_up.narration` não-vazio pra nunca marcar a entrada errada. **Prompt** (`agents/prompts.py`): seção nova "Viagem e encontros aleatórios" no `DM_SYSTEM_PROMPT` (usa `travel_hours`/`biome` num `move`, nunca rola nada sozinho) + `get_action_json_schema_description()` documentando o campo. **135 testes novos**: `test_world_tables_phase_40.py` (30 — loader/lookup/set_world_tables_root, cobertura d100/d20 sem gaps/overlaps em todas as tabelas, todo `monster.id` resolve via `get_monster`, todo item de loot resolve via `resolve_catalog_item`), `test_world_engine_phase_40.py` (23 — `roll_encounter`/`compute_loot`/`resolve_travel` com RNG determinístico via stand-in `FixedRandom` + `_stub_events`/monkeypatch local de `world.random` sem tocar o módulo `random` global, cobrindo multi-monstro, colisão de id, combate interrompendo o relógio, cooldown cross-call, reprodutibilidade com seed explícito, item de loot não resolvido não derruba o turno), `test_travel_action_phase_40.py` (17 — wiring `move`+`travel_hours`→`resolve_travel` via monkeypatch de `narrative.resolve_travel`, fallback pro stub antigo sem o param, forwarding de `combat_engine`, shape do mechanical, tag de `world_seed` na entrada certa, docs do prompt). **1841 testes passando** (1723 non-web + 118 web, dos quais 70 são novos desta fase). Mesmas 29 falhas + 59 erros pré-existentes de `tests/web/` (cache de `INVITE_CODE` do `.env` local vazando pra testes sem o autouse-fixture que desabilita o gate — confirmado idêntico num `git stash` da fase antes de qualquer mudança) — 0 regressões introduzidas. Ruff limpo em todos os arquivos da fase.
